global:
  resolve_timeout: 5m

route:
  receiver: "webhook"
  group_wait: 10s
  group_interval: 30s
  repeat_interval: 1m

receivers:
  - name: "webhook"
    webhook_configs:
      - url: "http://server:8000/alert"
        send_resolved: true
